import { ResultPlayDirective } from './result-play.directive';

describe('ResultPlayDirective', () => {
  it('should create an instance', () => {
    const directive = new ResultPlayDirective();
    expect(directive).toBeTruthy();
  });
});
